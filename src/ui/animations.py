\"\"\"UI-Animationen und Effekte\"\"\"\nfrom PyQt6.QtCore import Qt, QEvent, QPropertyAnimation, QSequentialAnimationGroup, QTimer, pyqtSignal\nfrom PyQt6.QtWidgets import QPushButton, QWidget\nfrom PyQt6.QtGui import QColor\n\n\nclass AnimatedButton(QPushButton):\n    \"\"\"Animierter Button mit Hover-Effekt\"\"\"\n    \n    def __init__(self, text: str = \"\"):\n        super().__init__(text)\n        self.is_hovered = False\n        self.setMinimumHeight(45)\n        self.setCursor(Qt.CursorShape.PointingHandCursor)\n    \n    def enterEvent(self, event: QEvent):\n        \"\"\"Hover-Effekt beim Betreten\"\"\"\n        self.is_hovered = True\n        self.update_style()\n        super().enterEvent(event)\n    \n    def leaveEvent(self, event: QEvent):\n        \"\"\"Hover-Effekt beim Verlassen\"\"\"\n        self.is_hovered = False\n        self.update_style()\n        super().leaveEvent(event)\n    \n    def update_style(self):\n        \"\"\"Aktualisiert den Stil basierend auf Hover-Status\"\"\"\n        pass\n\n\nclass PulseButton(AnimatedButton):\n    \"\"\"Button mit Puls-Animation\"\"\"\n    \n    clicked_signal = pyqtSignal()\n    \n    def __init__(self, text: str = \"\"):\n        super().__init__(text)\n        self.pulse_timer = QTimer()\n        self.pulse_timer.timeout.connect(self.animate_pulse)\n        self.scale = 1.0\n    \n    def animate_pulse(self):\n        \"\"\"Erstellt Puls-Effekt\"\"\"\n        self.scale = 1.0 + (0.05 * (1 if self.is_hovered else -1))\n        self.update()\n    \n    def enterEvent(self, event: QEvent):\n        \"\"\"Startet Puls bei Hover\"\"\"\n        super().enterEvent(event)\n        self.pulse_timer.start(50)\n    \n    def leaveEvent(self, event: QEvent):\n        \"\"\"Stoppt Puls beim Verlassen\"\"\"\n        super().leaveEvent(event)\n        self.pulse_timer.stop()\n        self.scale = 1.0\n        self.update()\n\n\nclass FadeWidget(QWidget):\n    \"\"\"Widget mit Fade-In/Out Animation\"\"\"\n    \n    def __init__(self):\n        super().__init__()\n        self.opacity = 0.0\n        self.fade_animation = QPropertyAnimation(self, b\"opacity\")\n        self.fade_animation.setDuration(300)\n    \n    def fade_in(self):\n        \"\"\"Fade-In Animation\"\"\"\n        self.fade_animation.setStartValue(0.0)\n        self.fade_animation.setEndValue(1.0)\n        self.fade_animation.start()\n    \n    def fade_out(self):\n        \"\"\"Fade-Out Animation\"\"\"\n        self.fade_animation.setStartValue(1.0)\n        self.fade_animation.setEndValue(0.0)\n        self.fade_animation.start()\n\n\nclass SlideAnimation:\n    \"\"\"Slide-Animation für Widgets\"\"\"\n    \n    @staticmethod\n    def create_slide_animation(widget: QWidget, duration: int = 300):\n        \"\"\"Erstellt eine Slide-Animation\"\"\"\n        animation = QPropertyAnimation(widget, b\"geometry\")\n        animation.setDuration(duration)\n        return animation\n"
